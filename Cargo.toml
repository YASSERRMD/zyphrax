[package]
name = "zyphrax"
version = "0.1.0"
edition = "2021"
authors = ["YASSERRMD"]
description = "High-performance compression engine (C kernel + Rust wrapper)"
license = "MIT"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
